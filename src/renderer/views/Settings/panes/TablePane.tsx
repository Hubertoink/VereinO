import React from 'react'
import { TablePaneProps } from '../types'

/**
 * TablePane - Table Column Configuration
 * 
 * Handles:
 * - Column visibility
 * - Column order (drag & drop)
 * - Preset configurations
 */
export function TablePane(props: TablePaneProps) {
  return (
    <div className="settings-pane">
      <h2>Tabellen-Einstellungen</h2>
      <p className="helper">Coming soon - Phase 4</p>
    </div>
  )
}
