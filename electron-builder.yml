appId: org.verein.finanzplaner
productName: VereinO
buildVersion: 3.1.1
icon: build/icon.png
files:
  - dist/**
  - dist-electron/**
asar: true
asarUnpack:
  - '**/*.node'
directories:
  output: release
win:
  target:
    - target: nsis
      arch: [x64]
  icon: build/Icon.ico
artifactName: '${productName}-Setup-${version}-${arch}.${ext}'
mac:
  category: public.app-category.finance
  icon: build/icon.png
  target:
    - target: dmg
      arch: [x64]
    - target: zip
      arch: [x64]
  artifactName: '${productName}-${version}-${arch}.${ext}'
linux:
  target: [AppImage]
  category: Finance
  icon: build/icon.png
  artifactName: '${productName}-${version}-${arch}.${ext}'
publish:
  provider: github
  owner: Hubertoink
  repo: VereinO
  releaseType: release
  channel: latest
  vPrefixedTagName: true
